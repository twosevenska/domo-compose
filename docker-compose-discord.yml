version: '2'
services:
    jmusicbot:
        image: twosevenska/jmusicbot:dev
        container_name: jmusicbot
        volumes:
            - ${DATA}/config/jmusicbot/config.txt:/bot/config.txt
        mem_limit: 1gb
        restart: always
